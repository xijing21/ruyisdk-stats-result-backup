# 强制所有 .sh 脚本使用 LF 换行符
*.sh text eol=lf

# 强制 JS/Node 脚本使用 LF 换行符
*.mjs text eol=lf
*.js text eol=lf

# 强制 YAML 配置文件使用 LF 换行符（防止 GitHub Actions 报错）
*.yml text eol=lf
*.yaml text eol=lf

# 默认行为
* text=auto